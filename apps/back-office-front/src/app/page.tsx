'use client';

import { Box } from '@mui/material';

export default function DashboardPage() {
  return (
    <Box
      sx={{
        p: 1,
        display: 'flex',
        justifyContent: 'center',
        alignContent: 'center',
      }}
    >
      test컴포넌트자리
    </Box>
  );
}
