'use client';

import { Box } from '@mui/material';

const DashboardPage = () => {
  return (
    <Box
      sx={{
        p: 1,
        m: 1,
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Back-Office
    </Box>
  );
};

export default DashboardPage;
