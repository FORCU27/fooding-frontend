'use client';

import { Box } from '@mui/material';

const UserListPage = () => {
  return (
    <Box
      sx={{
        p: 1,
        m: 1,
        height: '100%',
        display: 'flex',
      }}
    >
      유저목록
    </Box>
  );
};

export default UserListPage;
