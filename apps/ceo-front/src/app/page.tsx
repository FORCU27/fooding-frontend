'use client';

import { Box } from '@mui/material';

const DashboardPage = () => {
  return (
    <Box
      sx={{
        p: 1,
        m: 1,
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      ceo-front 메인화면
    </Box>
  );
};

export default DashboardPage;
